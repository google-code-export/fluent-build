#summary Project Roadmap and vision

= Introduction =
  # Get the basics down solid (directories, compilation, etc.) <--Done
  # File Tokenization <-- Done
  # Assembly Info Generation <-- Done
  # Look at other tools and add some nice helpers (unit test runners, zip support, etc.) <-- Done
  # Improving language, code base, and fixing bugs
  # Release
  # Write plugins for build servers (Team City, Cruise Control)
  # Adding language around more third party tools.
  # Updating and improving the database management code.
  # Look at implementing some conventions to make writing builds more repeatable

= Possible Third Party Tools =
  * unit test runners (mbunit, xunit, mstest)
  * IIS configuration
  * disco/wsdl tasks
  * SvcUtil.exe tasks (generate WCF proxies)
  * fxcop, msi building
  * scp, sftp, ftp
  * source control tasks (svn, git, tfs)
  * checksum utility